import React from "react";
import { InputGroup, Input, Button } from "reactstrap";

const AddTodoForm = () => {
  return (
    <InputGroup>
      <Input />
      <Button>Add</Button>
    </InputGroup>
  );
};

export default AddTodoForm;
